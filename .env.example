# Copy to .env (git-ignored) or export in your shell. Never commit real values.
# Used only by tools/migrate.ts and tools/reconcile.ts. The ServiceNow SDK itself
# stores instance credentials in its own credential store via `now-sdk auth`.
SN_INSTANCE_URL=https://dev399979.service-now.com
SERVICENOW_PDI_USERNAME=
SERVICENOW_PDI_PASSWORD=
